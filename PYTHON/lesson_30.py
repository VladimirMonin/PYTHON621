"""
Lesson 30

**init** — инициализирует объект после его создания; обычно здесь записывают начальные значения атрибутов экземпляра.

**str** — возвращает человекочитаемое строковое представление объекта; используется функцией str() и обычно print().

**len** — возвращает длину объекта; вызывается функцией len().

**bool** — определяет, считается ли объект истинным или ложным в if, while и при вызове bool().

**add** — определяет обычную операцию сложения: a + b.

**sub** — определяет обычную операцию вычитания: a - b.

**mul** — определяет обычную операцию умножения: a * b.

**truediv** — определяет обычное деление: a / b.

**floordiv** — определяет целочисленное деление: a // b.

**mod** — определяет получение остатка от деления: a % b.

**pow** — определяет возведение в степень: a ** b.

**matmul** — определяет оператор матричного умножения: a @ b; чаще используется математическими библиотеками.

**neg** — определяет унарный минус: -a.

**pos** — определяет унарный плюс: +a.

**abs** — определяет поведение функции abs(a).

**call** — позволяет использовать объект как функцию: obj().

**iter** — возвращает итератор объекта; позволяет использовать объект в for и других механизмах перебора.

**next** — возвращает следующий элемент итератора; вызывается функцией next() и циклом for.

**getitem** — определяет получение элемента через квадратные скобки: obj[key] или obj[index].

**setitem** — определяет изменение элемента через квадратные скобки: obj[key] = value.

**delitem** — определяет удаление элемента через квадратные скобки: del obj[key].

**contains** — определяет работу оператора in: value in obj.

**eq** — определяет сравнение на равенство: a == b.

**ne** — определяет сравнение на неравенство: a != b.

**lt** — определяет сравнение: a < b.

**le** — определяет сравнение: a <= b.

**gt** — определяет сравнение: a > b.

**ge** — определяет сравнение: a >= b.

**repr** — возвращает техническое строковое представление объекта; используется repr(), интерактивной консолью и часто предназначено для разработчика.

**hash** — возвращает хеш объекта; используется функцией hash() и необходим для использования объекта как ключа словаря или элемента set.

**enter** — выполняется при входе в блок with и возвращает объект, который будет доступен после as.

**exit** — выполняется при выходе из блока with; используется для освобождения ресурсов, закрытия файлов, соединений и обработки исключений.

**getattr** — вызывается, когда обычный поиск атрибута не смог найти запрошенный атрибут.

**getattribute** — вызывается при любом обращении к атрибуту объекта; позволяет полностью контролировать механизм получения атрибутов.

**setattr** — вызывается при присваивании значения атрибуту: obj.name = value.

**delattr** — вызывается при удалении атрибута: del obj.name.

**new** — непосредственно создаёт новый экземпляр класса; вызывается перед **init** и используется заметно реже.

**del** — может быть вызван при уничтожении объекта сборщиком мусора; обычно не рекомендуется использовать для критически важной очистки ресурсов.

**class_getitem** — определяет поведение класса при использовании квадратных скобок: MyClass[int]; часто используется при работе с типизацией и generic-классами.

**index** — позволяет объекту использоваться там, где Python требует именно целое число, например в срезах и некоторых встроенных функциях.

**int** — определяет преобразование объекта через int(obj).

**float** — определяет преобразование объекта через float(obj).

**complex** — определяет преобразование объекта через complex(obj).

**round** — определяет поведение round(obj).

**format** — определяет форматирование объекта через format() и f-строки с указанием формата.

**bytes** — определяет преобразование объекта через bytes(obj).

**reversed** — определяет работу reversed(obj).

**missing** — специальный метод подклассов dict; вызывается, если запрошенного ключа нет в словаре.

**copy** — определяет поведение поверхностного копирования объекта через copy.copy().

**deepcopy** — определяет поведение глубокого копирования через copy.deepcopy().

**await** — позволяет объекту использоваться в выражении await.

**aiter** — возвращает асинхронный итератор для конструкции async for.

**anext** — возвращает следующий элемент асинхронного итератора.

**aenter** — асинхронный аналог **enter**; используется конструкцией async with.

**aexit** — асинхронный аналог **exit**; выполняется при выходе из async with.

**get** — участвует в протоколе дескрипторов и определяет получение значения атрибута.

**set** — участвует в протоколе дескрипторов и определяет присваивание значения атрибуту.

**delete** — участвует в протоколе дескрипторов и определяет удаление атрибута.

**set_name** — вызывается при создании класса и сообщает дескриптору имя атрибута, под которым он был записан.

**instancecheck** — позволяет метаклассу изменить поведение isinstance().

**subclasscheck** — позволяет метаклассу изменить поведение issubclass().

**prepare** — метод метакласса, позволяющий определить пространство имён до выполнения тела создаваемого класса.

"""


class Car:
    threshold_gasoline = 4

    def __init__(self, model: str, year: int, weidth: int, length: int, gasoline_standart: int):
        self.model = model
        self.year = year
        self.weidth = weidth
        self.length = length
        self.gasoline_standart = gasoline_standart

    def __bool__(self):
        """
        Этот метод определяет то, как объект преобразуется в булево значение, когда мы используем функцию bool или помещаем объект в ветвление. Этот метод должен возвращать булево выражение. В нашем случае это может быть проверка на больше-меньше стандарта топлива, которое у конкретного экземпляра, и тем, что у нас записано как минимально допустимый стандарт топлива.
        """
        return self.gasoline_standart >= self.threshold_gasoline

    def __str__(self):
        """
        Этот метод отдаёт строку, которая возвращается, когда мы печатаем экземпляр этого класса.
        Фактически этот метод вызывает функцию `print` автоматически, когда мы помещаем в `print` экземпляр нашего класса.
        """
        return f"Автомобиль {self.model} - {self.year} года\nВес: {self.weidth}\nДлина: {self.length}мм\nСтандарт топлива: {self.gasoline_standart}\n============\n"

    def __len__(self):
        """
        Метод `len` вызывается автоматически в Python, когда экземпляр нашего класса помещается во встроенную функцию `len()`. Это может быть удобно, когда вы работаете с какими-то объектами, где важна эта характеристика.

        А также это позволяет использовать сортировку в формате: список экземпляров класса сортируется через `.sort(key=len)`.
        """
        return self.length



car_1 = Car("Лада Малина", 1995, 2000, 3000, 2)
car_2 = Car("Лада Клубника", 2026, 1300, 2600, 9)
car_3 = Car("Мерседес S600", 2021, 2200, 2800, 5)

if car_1:
    print(car_1, "Автомобиль годен для эксплуатации")
else:
    print(car_1, "Срочно уничтожить этот автомобиль")


print(len(car_1))

cars = [car_1, car_2, car_3]
cars.sort(key=len)
print(cars)
[print(car) for car in cars]

# print(car_1 + car_2) # TypeError: unsupported operand type(s) for +: 'Car' and 'Car'
# car_1() # TypeError: 'Car' object is not callable
# print(len(car_1)) # TypeError: object of type 'Car' has no len()
